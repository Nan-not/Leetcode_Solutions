1207. Unique Number of Occurrences
Solution:
class Solution(object):
    def uniqueOccurrences(self, arr):
        """
        :type arr: List[int]
        :rtype: bool
        """
        d={}
        for i in arr:
            if i in d:
                d[i]+=1
            else:
                d[i]=1
        b=[]
        for j in d:
            if d[j] in b:
                return False
            else:
                b.append(d[j])
        return True
